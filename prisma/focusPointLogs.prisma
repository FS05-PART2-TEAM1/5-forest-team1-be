model FocusPointLogs {
  id         String   @id @default(uuid())
  studyId    String
  points     Int
  startedAt  DateTime
  finishedAt DateTime
  duration   Int
  createdAt  DateTime @default(now())

  @@index([studyId])
}
